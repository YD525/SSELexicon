<Window x:Class="SSELex.ExtendWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SSELex"
        mc:Ignorable="d"
        Title="ExtendWin" Height="450" Width="300" Style="{DynamicResource DefWin}" Closing="Window_Closing" ShowInTaskbar="False">
    <Window.Resources>

        <Style x:Key="ListViewItemStyle" TargetType="{x:Type ListViewItem}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter  Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FontSize" Value="13.5"/>
            <Setter Property="Foreground"  Value="Black"/>
        </Style>

        <Storyboard x:Key="XTGlowLoop">
            <DoubleAnimation
          Storyboard.TargetName="LinearGradientBrushTranslate"
          Storyboard.TargetProperty="X"
          From="0"
          To="2000"
          Duration="0:0:30"
          RepeatBehavior="Forever" />
        </Storyboard>

        <Storyboard x:Key="ExpandMenu">
            <DoubleAnimation Storyboard.TargetName="LeftMenu"
                       Storyboard.TargetProperty="Height"
                       From="0" To="450" Duration="0:0:0.3"
                       AccelerationRatio="0.3" DecelerationRatio="0.7"/>
        </Storyboard>

        <Storyboard x:Key="CollapseMenu">
            <DoubleAnimation Storyboard.TargetName="LeftMenu"
                       Storyboard.TargetProperty="Height"
                       From="450" To="0" Duration="0:0:0.3"
                       AccelerationRatio="0.3" DecelerationRatio="0.7"/>
        </Storyboard>

        <Storyboard x:Key="ExpandStoryboard">
            <DoubleAnimation Storyboard.TargetName="NavPanel"
                       Storyboard.TargetProperty="Width"
                        From="0" To="50" Duration="0:0:0.3"
                       AccelerationRatio="0.3" DecelerationRatio="0.7"/>
        </Storyboard>

        <Storyboard x:Key="CollapseStoryboard">
            <DoubleAnimation Storyboard.TargetName="NavPanel"
                       Storyboard.TargetProperty="Width"
                       From="50" To="0" Duration="0:0:0.3"
                       AccelerationRatio="0.3" DecelerationRatio="0.7"/>
        </Storyboard>

    </Window.Resources>
    
    <Border Style="{DynamicResource BorderWinBackground}" Name="MainBackGround">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="8"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="25"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="25"/>
                    <ColumnDefinition Width="25"/>
                    <ColumnDefinition Width="25"/>
                    <ColumnDefinition Width="5"/>
                </Grid.ColumnDefinitions>
                <Label Name="Caption" Style="{DynamicResource CaptionFont}" Grid.Column="0">
                    Translation Finder
                </Label>

                <Border Style="{DynamicResource BlockButton}" Grid.Column="4" PreviewMouseDown="Min_PreviewMouseDown">
                    <ContentControl Style="{DynamicResource MinimizeIconStyle}"/>
                </Border>

                <Border Style="{DynamicResource BlockButton}" Grid.Column="5" PreviewMouseDown="AutoMax_PreviewMouseDown">
                    <ContentControl Style="{DynamicResource MaximizeIconStyle}"/>
                </Border>
                <Border Style="{DynamicResource BlockButton}" Grid.Column="6" PreviewMouseDown="Close_PreviewMouseDown">
                    <ContentControl Style="{DynamicResource CloseIconStyle}" Grid.Column="3"/>
                </Border>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <Label VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" Grid.Column="0">
                        From
                    </Label>

                    <Label VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" Grid.Column="1">
                        Key
                    </Label>

                    <Label VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" Grid.Column="2">
                        Translated
                    </Label>
                </Grid>

                <Grid Grid.Row="1" Style="{DynamicResource FrameStyle}">
                    <ScrollViewer Style="{DynamicResource for_scrollviewer}">
                        <StackPanel Orientation="Vertical" Name="MatchView">
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </Grid>
    </Border>

</Window>
