<Window x:Class="SSELex.UIManagement.RowStyleWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SSELex.UIManagement"
        mc:Ignorable="d"
        Title="RowStyleWin" Height="450" Width="800" Background="{x:Null}">

    <Grid>
        <StackPanel Orientation="Vertical" Margin="0 20 0 20">
            <StackPanel.Resources>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource for_scrollbar}" />
            </StackPanel.Resources>
            <!--45 DefLineHeight-->
            <Grid Height="40" Background="#FF2A2A2A" Name="LineGrid">
                <Border BorderThickness="0 0 0 2" BorderBrush="Black">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="0.5*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Ellipse  Width="10" Height="10" Fill="#FFE1E1E1">
                                </Ellipse>
                                <Label IsTabStop="False" Foreground="White" FontSize="13.9" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    
                                </Label>
                            </StackPanel>
                        </Grid>

                        <Grid Grid.Column="1" Margin="10 0 10 0">
                            <TextBox IsTabStop="False" AcceptsReturn="True" FontSize="13.9" HorizontalContentAlignment="Center" BorderThickness="0" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="White" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                               
                            </TextBox>
                        </Grid>

                        <Grid Grid.Column="2">
                            <TextBox IsTabStop="False" FontSize="13.9" Margin="5" HorizontalContentAlignment="Center" VerticalAlignment="Center" CaretBrush="#FFD4A000" SelectionTextBrush="#FF199EF5"  IsReadOnly="True" AcceptsReturn="True" BorderThickness="0" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" Background="{x:Null}" BorderBrush="{x:Null}"  Foreground="White" HorizontalAlignment="Stretch">
                               
                            </TextBox>
                        </Grid>

                        <Grid Grid.Column="3">
                            <Border Background="#FF1D1D1D" CornerRadius="5" Margin="5 2.5 5 2.5">
                                <TextBox IsTabStop="False" FontSize="13.9" Margin="5 0 5 0" HorizontalContentAlignment="Left" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" CaretBrush="#FFD4A000" SelectionTextBrush="#FF199EF5"  AcceptsReturn="True"  BorderThickness="0" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="White">

                                </TextBox>
                            </Border>
                          
                        </Grid>
                    </Grid>
                </Border>
            </Grid>

           
            
       

        </StackPanel>
    </Grid>
</Window>
